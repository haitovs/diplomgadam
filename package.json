{
  "name": "tagam-restaurant-finder",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant restaurant discovery platform for Ashgabat, with a store-owner portal and admin moderation.",
  "type": "module",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix frontend && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix server",
    "test": "npm run test --prefix server && npm run test --prefix frontend",
    "db:generate": "npm run db:generate --prefix server",
    "db:seed": "npm run db:seed --prefix server",
    "test:e2e": "playwright test -c e2e/playwright.config.ts"
  },
  "directories": {
    "doc": "docs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "concurrently": "^8.2.2",
    "drizzle-orm": "^0.45.2"
  }
}
